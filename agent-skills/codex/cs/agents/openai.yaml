interface:
  display_name: "Codesight (cs)"
  short_description: "Semantic code search (network-backed)"
  default_prompt: "Use $cs with network-enabled execution to index this repository and find the best code locations for my task."
