# TK-004

## Header
- Track: LSP Foundation
- Type: logic
- Modules: `pkg/lsp`

## Goal
Implement the reusable LSP JSON-RPC client layer needed by refs/callers/implements features.

## Files
- Allowed files:
  - `pkg/lsp/client.go`
  - `pkg/lsp/client_test.go`
  - `pkg/lsp/protocol_types.go`
  - `pkg/lsp/testdata/**`
- Out-of-scope files:
  - `cmd/cs/**`
  - `pkg/extract/**`
  - `pkg/indexer.go`

## Specs
- FR-04
- FR-06
- FR-07
- NFR-01
- NFR-05
- NFR-06

## Data Sources & Invariants
- Source of truth for transport behavior: LSP JSON-RPC protocol over stdin/stdout.
- Source of truth for server process ownership: lifecycle APIs from TK-003.
- Invariants:
  - Request IDs are unique per client session.
  - Response routing handles out-of-order messages.
  - Transport timeouts/error wrapping preserve actionable root causes.

## Implementation Notes
- Keep protocol structs centralized in `protocol_types.go` to prevent drift across refs/callers.
- Initialization handshake (`initialize`, `initialized`) must be explicit and test-covered.
- Shutdown/exit behavior should not leak goroutines or subprocess handles.
- Backward compatibility: no command-level behavior changes in this task.
- Add deferred integration case when real command wiring is not yet present:
  - `t.Skip("blocked by TK-006: refs command integration not wired")`

## Unit Tests
- Create/update test files:
  - `pkg/lsp/client_test.go`
- Coverage expectations:
  - Successful request/response roundtrip.
  - Initialization handshake path.
  - Timeout/error propagation.
  - Concurrent request handling.
  - Graceful shutdown semantics.

## Verification
- Auto:
  - `go test ./pkg/lsp -run Client`
  - `go build ./...`
  - `go test ./...`
- Human (if UI):
  - Not applicable.

## Done Criteria
- `pkg/lsp/client.go` supports stable JSON-RPC calls for downstream feature modules.
- Protocol tests cover concurrency and failure paths.
- `go build ./...` succeeds.
- `go test ./...` passes.
