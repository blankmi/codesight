# TK-002

## Header
- Track: Extraction
- Type: logic
- Modules: `cmd/cs`, `pkg/extract`

## Goal
Expose extraction in the CLI as `cs extract` with validated flags and stable output pass-through.

## Files
- Allowed files:
  - `cmd/cs/main.go`
  - `cmd/cs/main_test.go`
  - `cmd/cs/extract_command_test.go`
  - `pkg/extract/**` (only minimal API adjustments needed for CLI integration)
- Out-of-scope files:
  - `pkg/lsp/**`
  - `pkg/indexer.go`
  - `pkg/searcher.go`

## Specs
- FR-01
- FR-02
- FR-03
- NFR-01
- NFR-02
- NFR-05

## Data Sources & Invariants
- Source of truth for runtime behavior:
  - CLI flags (`-f`, `-s`, `--format`) override defaults.
  - Default format is `raw` when `--format` is not supplied.
- Invariants:
  - `-f` and `-s` are required for command execution.
  - Unknown `--format` values fail fast with non-zero exit.
  - Existing commands (`index`, `search`, `status`, `clear`) remain unchanged.

## Implementation Notes
- Wire command into root command registration in `init()`.
- Keep command signature exactly aligned with spec examples:
  - `cs extract -f <file> -s <symbol>`
  - `cs extract -f <file> -s <symbol> --format json`
- If extraction returns errors, preserve critical substrings (for tests and agent handling):
  - `symbol not found`
  - `unsupported`
- Backward compatibility: no CLI flag behavior regression on existing commands.
- If any command-level integration test depends on later LSP wiring, keep it compiling with:
  - `t.Skip("blocked by TK-006: refs command not wired")`

## Unit Tests
- Create/update test files:
  - `cmd/cs/extract_command_test.go`
  - `cmd/cs/main_test.go`
- Coverage expectations:
  - Required flag validation.
  - Default format behavior.
  - JSON format happy path.
  - Invalid format rejection.
  - Error propagation with required substrings.

## Verification
- Auto:
  - `go test ./cmd/cs -run Extract`
  - `go build ./...`
  - `go test ./...`
- Human (if UI):
  - Not applicable.

## Done Criteria
- `cs extract` is discoverable in CLI help and executable with required flags.
- Command outputs and errors match spec contracts.
- Existing commands keep passing tests.
- `go build ./...` succeeds.
- `go test ./...` passes.
