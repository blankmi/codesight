# TK-010

## Header
- Track: Docs and Migration
- Type: migration
- Modules: `README.md`, `agent-skills/**`, `docs/**`

## Goal
Finalize user-facing documentation and migration guidance for v2 command set and tool selection policy.

## Files
- Allowed files:
  - `README.md`
  - `agent-skills/codex/cs/SKILL.md`
  - `agent-skills/claude-code/cs/SKILL.md`
  - `agent-skills/gemini/gemini.skill`
  - `docs/prd/codesight-v2-unified-code-intelligence-cli/**`
- Out-of-scope files:
  - `pkg/**`
  - `cmd/cs/main.go`
  - `go.mod`

## Specs
- FR-09
- FR-10
- NFR-01
- NFR-02
- NFR-07

## Data Sources & Invariants
- Source of truth for shipped command set: implemented CLI commands in `cmd/cs`.
- Source of truth for migration messaging: spec FR-09 and PRD sections on tool selection.
- Invariants:
  - Docs state Grep remains preferred for lexical search.
  - Docs state `cs extract` is the recommended extraction path in this repo.
  - Docs do not claim stretch `implements` as shipped unless TK-009 is complete.
  - Docs explicitly state LSP runs as in-container child processes in Docker workflows.
  - Docs explicitly state `host.docker.internal` is for Milvus/Ollama network access, not LSP transport.
  - Docs treat `${CODESIGHT_STATE_DIR:-~/.codesight}` volume persistence as recommended for warm starts, not required for correctness.

## Implementation Notes
- Keep command examples consistent with exact signatures from `spec.md`.
- If stretch feature is deferred, explicitly label it as `stretch`/`deferred` in docs.
- Backward compatibility: avoid instructions that invalidate existing `cs index/search/status/clear` workflows.
- Include Docker runtime guidance with two modes: persisted state volume (warm reuse) and ephemeral state (cold start).
- Do not document remote/TCP LSP mode as supported in v2.
- This is a docs-only task; do not include incidental code changes.

## Unit Tests
- Test files:
  - No production test file changes expected in a docs-only slice.
- Coverage expectations:
  - Not applicable unless implementation code is touched; if code is touched, corresponding tests must be updated in this same task.

## Verification
- Auto:
  - `go build ./...`
  - `go test ./...`
- Human (if UI):
  - Setup: run `cs refs <symbol>` inside Docker with `CODESIGHT_STATE_DIR` mounted to a volume.
  - Verify: second invocation reuses warmed state/lifecycle artifacts.
  - Edge case: run without mounted state and verify cold start still succeeds with correct output/error contracts.

## Done Criteria
- README and agent skill docs accurately describe shipped v2 behavior and migration guidance.
- Docker runtime model, state-dir guidance, and host-network boundary are documented consistently across docs.
- Stretch status for `implements` is correctly represented.
- `go build ./...` succeeds.
- `go test ./...` passes.
